export const UI_ANIMATION_DELAYS = {
  MODAL_STATE_RESET: 200, // Small delay before clearing modal state to allow for exit animations
} as const;

export const UI_DISPLAY_DURATIONS = {
  SUCCESS_STATE: 1500, // Standard duration to show success feedback before auto-closing
} as const;
